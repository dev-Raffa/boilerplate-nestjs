services:
  db:
    image: postgres:15
    restart: always 
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: boilerplate
      POSTGRES_PASSWORD: boilerplate123
      POSTGRES_DB: boilerplate

